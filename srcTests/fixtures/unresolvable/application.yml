propertySources:
    -   name: env
        provider: winterBootTests\Support\StubPropertySource
        dataset: env
    -   name: ini
        provider: winterBootTests\Support\StubPropertySource
        dataset: ini

broken: "$env.ghost || $ini.ghost"
